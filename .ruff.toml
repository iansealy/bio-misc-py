select = ["ALL"]

# Ignore incompatible rules
ignore = [
    "D203", # one-blank-line-before-class
    "D213", # multi-line-summary-second-line
]

[per-file-ignores]
# Allow assert in tests
"test_*.py" = ["S101"]
# Allow print in script that writes to STDOUT
"mergeduplicates.py" = ["T201"]
